<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title>AuctioX feed - Newest listings</title>
  <link href="{{domain}}/feed"/>
  <updated>{{ dateUpdated }}</updated>
  <id>fbc23082-fcf0-4f7c-9f63-f6398feed8b4</id>

  {% for product in products%}
  <entry>
    <title>{{product.title}}</title>

    <link href="{{domain}}/product?prodid={{ product.productID }}"/>

    <author>{{product.ownerID}}</author>

    <content type="text">
        Condition: {{product.condition}}
        Price: {{product.price}} {{product.currency}}
        Expiration date: {{product.dateExpires}}
    </content>

    <summary>
        {{product.desc[0:200]}}
    </summary>

    <category term="{{product.category}}" label="Category: {{product.category}}"/>

    <published>
        {{product.dateAdded}}
    </published>
  </entry>
  {% endfor %}

</feed>
